services:
  docs-ermis:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    image: thangbelo/docs-ermis:v1.0.6
    container_name: docs-ermis
    ports:
      - "127.0.0.1:3333:3334"
    restart: always