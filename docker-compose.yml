services:
  docs-ermis:
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    image: registry.ermis.network/ermis/docs-ermis:v1.1.7
    container_name: docs-ermis
    ports:
      - "3333:3334"
    restart: always